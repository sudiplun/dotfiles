walkerselection_wrap = false # wrap list if at bottom or top

[placeholders]
"default" = { input = "Search", list = "No Results" } # placeholders for input and empty list, key is the providers name, so f.e. "desktopapplications" or "menus:other"

[providers]
# LOAD EXTRA PROVIDERS DEFAULT
default = ["desktopapplications"]

[[providers.prefixes]]
prefix = "2"
provider = "todo"

[[providers.prefixes]]
prefix = "1"
provider = "clipboard"

[[providers.prefixes]]
prefix = "@"
provider = "archlinuxpkgs"

[providers.clipboard]
#time_format = "%d.%m. - %H:%M" # format for the clipboard item date
time_foramt = "relative"
